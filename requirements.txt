fastapi>=0.111
uvicorn[standard]>=0.29
pydantic>=2.7
pillow>=10.3
numpy>=1.26
opencv-python-headless>=4.9
lxml>=5.2
httpx>=0.27
imagehash>=4.3
scikit-learn>=1.4
anthropic>=0.40          # VLM agent G1–G6 (cần ANTHROPIC_API_KEY)
# OCR backends (optional — cần cài thủ công nếu muốn dùng):
# paddlepaddle>=2.6; paddleocr>=2.7
# pytesseract>=0.3  (cần Tesseract binary: apt install tesseract-ocr)
