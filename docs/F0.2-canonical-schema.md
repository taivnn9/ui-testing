# F0.2 — Canonical Schema v1

> **TL;DR:** Contract dữ liệu chung mà mọi analyzer ghi vào và mọi downstream (Rule Engine, agent reasoning Codex/Cline) đọc ra. Toạ độ nội bộ là **device px** (quy đổi xem F0.4); mọi thay đổi schema phải bump version.

---

## 1. Cấu trúc tổng quan

`CanonicalDoc` gồm:

| Field | Nội dung |
|---|---|
| `screen` | meta màn hình & mode vận hành |
| `image` | đường dẫn + kích thước ảnh gốc |
| `elements[]` | phần tử (cây phẳng, quan hệ qua `parent/children`) |
| `relations[]` | quan hệ tương đối tiền-tính (overlap, gap, align…) |
| `candidate_issues[]` | output Rule Engine, đưa cho agent xác nhận |

---

## 2. `screen`

```jsonc
{
  "id": "scr_001",
  "platform": "android | ios | web",
  "route": "/checkout",                              // tùy chọn
  "viewport": { "w": 390, "h": 844, "dpr": 3.0 },    // device px = CSS px × dpr
  "safe_area": { "top": 47, "bottom": 34, "left": 0, "right": 0 },  // device px
  "theme": "light | dark | system",
  "locale": "vi-VN",
  "font_scale": 1.0,                                 // hệ số phóng chữ trợ năng
  "ts": "2026-05-28T10:00:00Z"
}
```

## 3. `image`

```jsonc
{
  "full": "screens/scr_001.png",   // đường dẫn tương đối từ gốc request
  "w": 1170,                       // device px (= viewport.w × dpr)
  "h": 2532
}
```

---

## 4. `elements[]`

```jsonc
{
  "id": "e12",
  "role": "button | text | image | icon | input | toggle | list | container | tab | nav | modal | skeleton | spinner | divider | unknown",
  "source": "vision | pixel",   // vision = OCR/CV, pixel = đo từ ảnh
  "confidence": 0.95,           // [0,1]; độ chắc phát hiện element

  "bbox": { "x": 24, "y": 200, "w": 342, "h": 48 },                 // device px
  "bbox_norm": { "x": 0.02, "y": 0.08, "w": 0.29, "h": 0.02 },      // / image.w,h

  "parent": "e3",               // null nếu gốc
  "children": ["e13", "e14"],
  "z": 2,                       // z-order (0 = dưới cùng)

  "text": "Thêm vào giỏ",
  "text_truncated": false,

  "style": {
    "font_size": 16,            // device px (A5 ước lượng từ bbox height)
    "font_family": "Roboto",
    "color": "#FFFFFF",         // hex 6 ký tự (A4 đo pixel)
    "bg_color": "#E53935",
    "contrast_ratio": 4.8,      // WCAG (A4 tính từ pixel)
    "opacity": 1.0,
    "border_radius": 8,
    "dark_mode_ok": true,       // null nếu theme≠dark; false = hardcode màu sáng
    "_sources": { "contrast_ratio": "pixel", "font_size": "vision" }  // override source theo field
  },

  // chỉ điền khi role = "image"
  "image_meta": {
    "intrinsic_w": null, "intrinsic_h": null,    // luôn null (vision-only)
    "displayed_w": 342, "displayed_h": 228,      // device px — từ bbox A3
    "scale_mode": null,                          // "unknown" (không biết object-fit)
    "blur_score": 82.4,                          // Laplacian variance (A7); null nếu crop < 32px
    "is_broken": false,                          // broken-image pattern (A7/A9)
    "is_partial_load": false,                    // ảnh load dở (A9)
    "intrinsic_unavailable": true                // luôn true ở Phase 1
  },

  "interactive": true,
  "touch_target": { "w": 342, "h": 48 },         // device px (có thể > bbox nếu có padding)
  "visible": true,
  "clipped": false,                              // bị container cắt
  "offscreen": false,                            // ngoài viewport
  "crop": "crops/e12.png"                        // null nếu chưa crop
}
```

### 4.1. Enum `role`

| Giá trị | Mô tả |
|---|---|
| `button` | Nút bấm (gồm FAB, chip) |
| `text` | Văn bản thuần |
| `image` | Ảnh (img, background-image có nội dung) |
| `icon` | Icon (svg, icon-font, image nhỏ trang trí) |
| `input` | Text field, textarea, search bar |
| `toggle` | Switch, checkbox, radio |
| `list` | List/RecyclerView/FlatList container |
| `container` | Div/View không thuộc loại trên |
| `tab` | Tab item |
| `nav` | Navigation bar, bottom bar, drawer |
| `modal` | Dialog, bottom sheet, toast |
| `skeleton` | Skeleton loading placeholder |
| `spinner` | Loading spinner / progress |
| `unknown` | Không xác định |

### 4.2. `style._sources`

Dict theo dõi nguồn ở cấp field (thay vì nhân đôi field). Downstream đọc `_sources[field]` để biết độ tin cậy; nếu thiếu key → dùng `element.source`.

---

## 5. `relations[]`

Tiền-tính bởi A0 Normalize — downstream không cần tự suy từ toạ độ thô.

```jsonc
{
  "a": "e7",
  "rel": "left_of | right_of | above | below | contains | overlaps | sibling",
  "b": "e8",
  "gap": 8,     // device px; 0 nếu kề; âm = overlap (độ xuyên)
  "iou": 0.0    // > 0 nếu overlap
}
```

| `rel` | Điều kiện |
|---|---|
| `left_of` | a.right ≤ b.x (có gap) |
| `right_of` | a.x ≥ b.right |
| `above` | a.bottom ≤ b.y |
| `below` | a.y ≥ b.bottom |
| `contains` | a bao hoàn toàn b |
| `overlaps` | iou > 0, không contains |
| `sibling` | cùng parent, không overlap |

---

## 6. `candidate_issues[]`

Output Rule Engine. Agent reasoning (Codex/Cline) đọc để xác nhận/bác bỏ và bổ sung lỗi cần phán đoán.

```jsonc
{
  "rule": "touch_target_min",      // ID rule (xem F0.4)
  "element": "e12",                // null nếu lỗi cấp màn hình
  "severity": "high",              // mức baseline Rule Engine gán
  "severity_range": { "min": "medium", "max": "critical" },  // agent chốt mức cuối trong range
  "confidence": 0.92,              // Rule Engine tất định thường = 1.0
  "detail": "touch_target.h=30px < 48dp threshold (144px)",
  "evidence": { "bbox": { "x": 24, "y": 200, "w": 342, "h": 30 }, "crop": "crops/e12.png" }
}
```

### 6.1. Severity (5 mức)

| Mức | Định nghĩa |
|---|---|
| `critical` | Chặn hoàn toàn: không hoàn thành tác vụ / mất chức năng chính |
| `high` | Nặng: ảnh hưởng rõ thao tác & đọc hiểu, vẫn xoay sở được |
| `medium` | Gây khó chịu / giảm tin cậy, không cản trở tác vụ |
| `low` | Nhỏ, phải để ý mới thấy |
| `trivial` | Thẩm mỹ cực nhỏ, gần như bỏ qua |

> 4 mức cũ (`blocker/major/minor/cosmetic`) đã bỏ — mọi rule và agent dùng 5 mức trên.

---

## 7. Pydantic v2 skeleton

```python
from __future__ import annotations
from typing import Literal, Optional
from pydantic import BaseModel, Field

SourceType = Literal["vision", "pixel"]
PlatformType = Literal["android", "ios", "web"]
SeverityType = Literal["critical", "high", "medium", "low", "trivial"]
RelType = Literal["left_of", "right_of", "above", "below", "contains", "overlaps", "sibling"]
RoleType = Literal[
    "button", "text", "image", "icon", "input", "toggle",
    "list", "container", "tab", "nav", "modal", "skeleton", "spinner", "divider", "unknown"
]


class BBox(BaseModel):
    x: float; y: float; w: float; h: float

class Viewport(BaseModel):
    w: int; h: int; dpr: float

class SafeArea(BaseModel):
    top: int; bottom: int; left: int; right: int

class Screen(BaseModel):
    id: str
    platform: PlatformType
    route: Optional[str] = None
    viewport: Viewport
    safe_area: SafeArea
    theme: Literal["light", "dark", "system"] = "light"
    locale: str = "en-US"
    font_scale: float = 1.0
    ts: Optional[str] = None

class Image(BaseModel):
    full: str; w: int; h: int

class StyleSources(BaseModel):
    font_size: Optional[SourceType] = None
    font_family: Optional[SourceType] = None
    color: Optional[SourceType] = None
    bg_color: Optional[SourceType] = None
    contrast_ratio: Optional[SourceType] = None
    opacity: Optional[SourceType] = None
    border_radius: Optional[SourceType] = None

class Style(BaseModel):
    font_size: Optional[float] = None
    font_family: Optional[str] = None
    color: Optional[str] = None
    bg_color: Optional[str] = None
    contrast_ratio: Optional[float] = None
    opacity: float = 1.0
    border_radius: Optional[float] = None
    dark_mode_ok: Optional[bool] = None   # False = hardcode màu sáng trong dark theme
    sources: Optional[StyleSources] = Field(None, alias="_sources")

class ImageMeta(BaseModel):
    intrinsic_w: Optional[int] = None; intrinsic_h: Optional[int] = None
    displayed_w: Optional[int] = None;  displayed_h: Optional[int] = None
    scale_mode: Optional[Literal["fill","fit","stretch","tile","none"]] = None
    blur_score: Optional[float] = None          # Laplacian variance — A7
    is_broken: bool = False                      # broken-image pattern — A7/A9
    is_partial_load: bool = False                # load dở — A9
    intrinsic_unavailable: bool = False          # luôn True ở Phase 1

class TouchTarget(BaseModel):
    w: float; h: float

class Element(BaseModel):
    id: str
    role: RoleType
    source: SourceType
    confidence: float = 1.0
    bbox: BBox
    bbox_norm: BBox
    parent: Optional[str] = None
    children: list[str] = []
    z: int = 0
    text: Optional[str] = None
    text_truncated: bool = False
    style: Optional[Style] = None
    image_meta: Optional[ImageMeta] = None
    interactive: bool = False
    interactive_confidence: float = 0.0          # A12: độ chắc [0,1]
    interactive_signals: list[str] = []          # A12: vd ["rounded_rect","action_label"]
    touch_target: Optional[TouchTarget] = None
    visible: bool = True
    clipped: bool = False
    offscreen: bool = False
    crop: Optional[str] = None

class Relation(BaseModel):
    a: str; rel: RelType; b: str
    gap: float = 0.0
    iou: float = 0.0

class SeverityRange(BaseModel):
    min: SeverityType; max: SeverityType

class Evidence(BaseModel):
    bbox: Optional[BBox] = None
    crop: Optional[str] = None

class CandidateIssue(BaseModel):
    rule: str
    element: Optional[str] = None
    severity: SeverityType
    severity_range: SeverityRange
    confidence: float = 1.0
    detail: str = ""
    evidence: Optional[Evidence] = None

class CanonicalDoc(BaseModel):
    screen: Screen
    image: Image
    elements: list[Element] = []
    relations: list[Relation] = []
    candidate_issues: list[CandidateIssue] = []
```

---

## 8. Ghi chú cho analyzer

- Analyzer **không ghi thẳng vào `candidate_issues`** — đó là phần của Rule Engine. Analyzer chỉ điền `elements[]` (và `relations[]` nếu là A0).
- Field không xác định được → để `null`, **không bịa**.
- `confidence` cấp element = độ chắc của **phát hiện element**, không phải của từng field style.
- `source` cấp element: `"vision"` khi phát hiện qua OCR/CV; `"pixel"` khi đo trực tiếp từ ảnh.
- Khi analyzer bổ sung field style cho element đã có → cập nhật field + ghi `_sources`.
