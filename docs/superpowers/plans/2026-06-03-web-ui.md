# Web UI Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Thêm giao diện web (tải ảnh → Phân tích → danh sách lỗi + chi tiết + overlay lỗi trên ảnh clone), tích hợp vào FastAPI service sẵn có.

**Architecture:** 1 trang tĩnh (HTML/CSS/JS vanilla, không Node/build) đặt trong `src/ui_defect/web/`. FastAPI mount `/static` và phục vụ `GET /` trả `index.html`. Frontend gọi nguyên `POST /analyze`, render summary + list, vẽ overlay bbox (scale theo `displayedWidth/naturalWidth`) với số thứ tự + màu severity, liên kết 2 chiều list↔box, filter theo severity.

**Tech Stack:** FastAPI + StaticFiles (đã có), vanilla JS (fetch + FormData), CSS thuần. Test backend bằng `fastapi.testclient.TestClient` (httpx đã là dep). Frontend verify thủ công.

**Spec nguồn:** `docs/F2.0-web-ui.md`

---

### Task 1: Backend serve trang web (static mount + GET /)

**Files:**
- Modify: `src/ui_defect/api/main.py`
- Create: `src/ui_defect/web/index.html` (placeholder tối thiểu ở task này; nội dung đầy đủ ở Task 4)
- Create: `src/ui_defect/web/static/.gitkeep`
- Test: `tests/unit/test_web_serving.py`

- [ ] **Step 1: Tạo file web tối thiểu để serve được**

Create `src/ui_defect/web/index.html`:
```html
<!doctype html>
<html lang="vi">
<head><meta charset="utf-8"><title>UI Defect Analyzer</title></head>
<body><div id="app">placeholder</div></body>
</html>
```

Create `src/ui_defect/web/static/.gitkeep` (file rỗng, để git track thư mục).

- [ ] **Step 2: Viết test fail trước**

Create `tests/unit/test_web_serving.py`:
```python
from fastapi.testclient import TestClient

from ui_defect.api.main import app

client = TestClient(app)


def test_index_served_at_root():
    resp = client.get("/")
    assert resp.status_code == 200
    assert "text/html" in resp.headers["content-type"]
    assert "UI Defect Analyzer" in resp.text


def test_static_mount_exists():
    # /static được mount; file không tồn tại trả 404 (không phải route thiếu)
    resp = client.get("/static/does-not-exist.js")
    assert resp.status_code == 404


def test_health_still_works():
    resp = client.get("/health")
    assert resp.status_code == 200
    assert resp.json()["status"] == "ok"
```

- [ ] **Step 3: Chạy test, xác nhận FAIL**

Run: `pytest tests/unit/test_web_serving.py -v`
Expected: `test_index_served_at_root` FAIL (404 — chưa có route `/`).

- [ ] **Step 4: Sửa main.py để serve web**

Trong `src/ui_defect/api/main.py`, thêm import ở đầu file (cạnh các import FastAPI):
```python
from pathlib import Path

from fastapi.responses import FileResponse
from fastapi.staticfiles import StaticFiles
```

Ngay sau khối tạo `app = FastAPI(...)` (sau dòng `)` đóng FastAPI, trước `_MAX_IMAGE_BYTES`), thêm:
```python
_WEB_DIR = Path(__file__).resolve().parent.parent / "web"

app.mount("/static", StaticFiles(directory=_WEB_DIR / "static"), name="static")


@app.get("/", include_in_schema=False)
def index():
    return FileResponse(_WEB_DIR / "index.html")
```

- [ ] **Step 5: Chạy test, xác nhận PASS**

Run: `pytest tests/unit/test_web_serving.py -v`
Expected: cả 3 test PASS.

- [ ] **Step 6: Commit**

```bash
git add src/ui_defect/api/main.py src/ui_defect/web/ tests/unit/test_web_serving.py
git commit -m "feat(web): serve trang web tĩnh qua FastAPI (GET / + /static)"
```

---

### Task 2: CSS — layout 2 cột, bảng màu severity, responsive

**Files:**
- Create: `src/ui_defect/web/static/style.css`

Không có unit test cho CSS — verify thủ công ở Task 5. Đây là task tạo file nội dung hoàn chỉnh.

- [ ] **Step 1: Viết style.css đầy đủ**

Create `src/ui_defect/web/static/style.css`:
```css
:root {
  --sev-critical: #e53935;
  --sev-high: #fb8c00;
  --sev-medium: #fdd835;
  --sev-low: #42a5f5;
  --sev-trivial: #9e9e9e;
  --bg: #f5f6f8;
  --panel: #ffffff;
  --border: #e0e3e8;
  --text: #1f2329;
  --muted: #6b7280;
}
* { box-sizing: border-box; }
body {
  margin: 0; font-family: system-ui, -apple-system, "Segoe UI", Roboto, sans-serif;
  background: var(--bg); color: var(--text);
}
header {
  display: flex; align-items: center; justify-content: space-between;
  padding: 12px 20px; background: var(--panel); border-bottom: 1px solid var(--border);
}
header h1 { font-size: 18px; margin: 0; }
.uploadbar {
  padding: 16px 20px; background: var(--panel); border-bottom: 1px solid var(--border);
}
.dropzone {
  border: 2px dashed var(--border); border-radius: 10px; padding: 24px;
  text-align: center; color: var(--muted); cursor: pointer; transition: .15s;
}
.dropzone.dragover { border-color: var(--sev-low); background: #f0f7ff; color: var(--text); }
.controls { margin-top: 12px; display: flex; gap: 12px; align-items: center; flex-wrap: wrap; }
.controls.hidden { display: none; }
.btn {
  background: var(--sev-low); color: #fff; border: 0; border-radius: 8px;
  padding: 10px 20px; font-size: 14px; cursor: pointer;
}
.btn:disabled { opacity: .5; cursor: not-allowed; }
.btn-secondary { background: transparent; color: var(--sev-low); border: 1px solid var(--sev-low); }
.layout { display: flex; gap: 16px; padding: 16px 20px; align-items: flex-start; }
.col-image { flex: 1 1 60%; min-width: 0; }
.col-results { flex: 1 1 40%; min-width: 280px; }
@media (max-width: 900px) { .layout { flex-direction: column; } }

.canvas-wrap { position: relative; display: inline-block; max-width: 100%; }
.canvas-wrap img { max-width: 100%; height: auto; display: block; border: 1px solid var(--border); }
.overlay {
  position: absolute; border: 2px solid; border-radius: 3px; box-sizing: border-box;
  pointer-events: auto; cursor: pointer;
}
.overlay .marker {
  position: absolute; top: -2px; left: -2px; transform: translate(-50%, -50%);
  min-width: 20px; height: 20px; border-radius: 50%; color: #fff; font-size: 12px;
  font-weight: 700; display: flex; align-items: center; justify-content: center;
  padding: 0 4px;
}
.overlay.active { box-shadow: 0 0 0 3px rgba(0,0,0,.25); }

.summary { background: var(--panel); border: 1px solid var(--border); border-radius: 10px; padding: 14px; margin-bottom: 12px; }
.sev-chips { display: flex; gap: 8px; flex-wrap: wrap; margin-top: 8px; }
.sev-chip {
  display: inline-flex; align-items: center; gap: 6px; padding: 4px 10px;
  border-radius: 999px; font-size: 13px; cursor: pointer; border: 1px solid var(--border);
  user-select: none;
}
.sev-chip .dot { width: 10px; height: 10px; border-radius: 50%; }
.sev-chip.off { opacity: .4; text-decoration: line-through; }

.issue-list { display: flex; flex-direction: column; gap: 8px; }
.issue {
  background: var(--panel); border: 1px solid var(--border); border-left-width: 4px;
  border-radius: 8px; padding: 10px 12px; cursor: pointer;
}
.issue.active { box-shadow: 0 0 0 2px var(--sev-low); }
.issue.hidden { display: none; }
.issue-head { display: flex; align-items: center; gap: 8px; }
.issue-num {
  min-width: 22px; height: 22px; border-radius: 50%; color: #fff; font-size: 12px;
  font-weight: 700; display: inline-flex; align-items: center; justify-content: center;
}
.issue-title { font-size: 14px; font-weight: 600; flex: 1; }
.issue-conf { font-size: 12px; color: var(--muted); }
.badge { font-size: 11px; padding: 2px 8px; border-radius: 4px; color: #fff; text-transform: uppercase; }
.issue-detail { margin-top: 8px; font-size: 13px; color: var(--text); display: none; }
.issue.active .issue-detail { display: block; }
.issue-detail dl { display: grid; grid-template-columns: auto 1fr; gap: 4px 10px; margin: 6px 0 0; }
.issue-detail dt { color: var(--muted); }
.tags { display: flex; gap: 6px; flex-wrap: wrap; margin-top: 6px; }
.tag { font-size: 11px; background: var(--bg); border: 1px solid var(--border); border-radius: 4px; padding: 1px 6px; color: var(--muted); }

.banner { padding: 12px 16px; border-radius: 8px; margin-bottom: 12px; font-size: 14px; }
.banner.error { background: #fdecea; color: #b71c1c; border: 1px solid #f5c6cb; }
.empty { text-align: center; color: var(--muted); padding: 40px 0; font-size: 16px; }
.spinner {
  display: inline-block; width: 16px; height: 16px; border: 2px solid rgba(255,255,255,.4);
  border-top-color: #fff; border-radius: 50%; animation: spin .7s linear infinite; vertical-align: -2px;
}
@keyframes spin { to { transform: rotate(360deg); } }
.hidden { display: none; }
```

- [ ] **Step 2: Commit**

```bash
git add src/ui_defect/web/static/style.css
git commit -m "feat(web): CSS layout 2 cột + bảng màu severity + responsive"
```

---

### Task 3: JS — app.js (upload, gọi /analyze, render, overlay, liên kết 2 chiều, filter)

**Files:**
- Create: `src/ui_defect/web/static/app.js`

Logic frontend; verify thủ công ở Task 5. Task tạo file nội dung hoàn chỉnh.

- [ ] **Step 1: Viết app.js đầy đủ**

Create `src/ui_defect/web/static/app.js`:
```javascript
"use strict";

const SEV = {
  critical: { color: "#e53935", rank: 5 },
  high:     { color: "#fb8c00", rank: 4 },
  medium:   { color: "#fdd835", rank: 3 },
  low:      { color: "#42a5f5", rank: 2 },
  trivial:  { color: "#9e9e9e", rank: 1 },
};
const SEV_ORDER = ["critical", "high", "medium", "low", "trivial"];

const state = {
  file: null,
  objectUrl: null,
  issues: [],          // [{...issue, _num}]
  activeId: null,
  hiddenSev: new Set(),
};

const $ = (sel) => document.querySelector(sel);

function init() {
  const dz = $("#dropzone");
  const fileInput = $("#fileInput");
  dz.addEventListener("click", () => fileInput.click());
  dz.addEventListener("dragover", (e) => { e.preventDefault(); dz.classList.add("dragover"); });
  dz.addEventListener("dragleave", () => dz.classList.remove("dragover"));
  dz.addEventListener("drop", (e) => {
    e.preventDefault(); dz.classList.remove("dragover");
    if (e.dataTransfer.files.length) onFile(e.dataTransfer.files[0]);
  });
  fileInput.addEventListener("change", () => {
    if (fileInput.files.length) onFile(fileInput.files[0]);
  });
  $("#analyzeBtn").addEventListener("click", analyze);
  $("#toggleAdvanced").addEventListener("click", () => {
    $("#advanced").classList.toggle("hidden");
  });
  window.addEventListener("resize", positionOverlays);
}

function onFile(file) {
  if (!file.type.startsWith("image/")) {
    showBanner("File không phải ảnh hợp lệ.");
    return;
  }
  clearBanner();
  state.file = file;
  if (state.objectUrl) URL.revokeObjectURL(state.objectUrl);
  state.objectUrl = URL.createObjectURL(file);
  $("#dropzone").textContent = file.name;
  const img = $("#screenshot");
  img.onload = positionOverlays;
  img.src = state.objectUrl;
  $("#canvasWrap").classList.remove("hidden");
  $("#analyzeBtn").disabled = false;
  // reset kết quả cũ
  state.issues = [];
  renderResults();
  clearOverlays();
}

async function analyze() {
  if (!state.file) { showBanner("Hãy chọn ảnh trước."); return; }
  clearBanner();
  const btn = $("#analyzeBtn");
  btn.disabled = true;
  btn.innerHTML = '<span class="spinner"></span> Đang phân tích…';

  const fd = new FormData();
  fd.append("screenshot", state.file);
  fd.append("platform", $("#platform").value);
  fd.append("min_severity", $("#minSeverity").value);
  fd.append("min_confidence", $("#minConfidence").value);
  fd.append("run_vlm", $("#runVlm").checked ? "true" : "false");

  try {
    const resp = await fetch("/analyze", { method: "POST", body: fd });
    if (!resp.ok) {
      const detail = await safeDetail(resp);
      showBanner(errorMessage(resp.status, detail));
      return;
    }
    const data = await resp.json();
    state.issues = data.issues.map((it, i) => ({ ...it, _num: i + 1 }));
    state.issues.sort((a, b) =>
      (SEV[b.severity]?.rank || 0) - (SEV[a.severity]?.rank || 0) ||
      b.confidence - a.confidence);
    state.issues.forEach((it, i) => { it._num = i + 1; });
    renderSummary(data.summary);
    renderResults();
    renderOverlays();
  } catch (e) {
    showBanner("Không kết nối được server.");
  } finally {
    btn.disabled = false;
    btn.textContent = "Phân tích";
  }
}

async function safeDetail(resp) {
  try { return (await resp.json()).detail || ""; } catch { return ""; }
}

function errorMessage(status, detail) {
  if (status === 413) return "Ảnh quá lớn (vượt giới hạn).";
  if (status === 400) return "File không phải ảnh hợp lệ.";
  if (status === 500) return "Phân tích thất bại: " + detail;
  return "Lỗi không xác định (" + status + ").";
}

function renderSummary(summary) {
  const el = $("#summary");
  if (!summary) { el.innerHTML = ""; return; }
  const chips = SEV_ORDER.map((s) => {
    const n = (summary.by_severity && summary.by_severity[s]) || 0;
    const off = state.hiddenSev.has(s) ? " off" : "";
    return `<span class="sev-chip${off}" data-sev="${s}">
      <span class="dot" style="background:${SEV[s].color}"></span>${s} ${n}</span>`;
  }).join("");
  el.innerHTML = `<div><strong>Tổng ${summary.total_issues} lỗi</strong>
    <span class="issue-conf"> · confidence TB ${(summary.confidence_avg || 0).toFixed(2)}</span></div>
    <div class="sev-chips">${chips}</div>`;
  el.querySelectorAll(".sev-chip").forEach((chip) => {
    chip.addEventListener("click", () => {
      const s = chip.dataset.sev;
      if (state.hiddenSev.has(s)) state.hiddenSev.delete(s); else state.hiddenSev.add(s);
      chip.classList.toggle("off");
      renderResults(); renderOverlays();
    });
  });
}

function renderResults() {
  const list = $("#issueList");
  const visible = state.issues.filter((it) => !state.hiddenSev.has(it.severity));
  if (state.issues.length === 0) {
    list.innerHTML = `<div class="empty">Chưa có kết quả. Tải ảnh và ấn Phân tích.</div>`;
    return;
  }
  if (visible.length === 0) {
    list.innerHTML = `<div class="empty">Không phát hiện lỗi 🎉</div>`;
    return;
  }
  list.innerHTML = visible.map((it) => {
    const c = SEV[it.severity]?.color || "#999";
    const active = it.id === state.activeId ? " active" : "";
    const ev = it.evidence || {};
    const measured = ev.measured_value ? `<dt>Đo được</dt><dd>${esc(ev.measured_value)}</dd>` : "";
    const expected = ev.expected_value ? `<dt>Kỳ vọng</dt><dd>${esc(ev.expected_value)}</dd>` : "";
    const role = it.element_role ? `<dt>Element</dt><dd>${esc(it.element_role)}</dd>` : "";
    const txt = it.element_text ? `<dt>Text</dt><dd>${esc(it.element_text)}</dd>` : "";
    const tags = (it.tags || []).map((t) => `<span class="tag">${esc(t)}</span>`).join("");
    return `<div class="issue${active}" data-id="${it.id}" style="border-left-color:${c}">
      <div class="issue-head">
        <span class="issue-num" style="background:${c}">${it._num}</span>
        <span class="issue-title">${esc(it.title)}</span>
        <span class="badge" style="background:${c}">${it.severity}</span>
      </div>
      <div class="issue-conf">type: ${esc(it.issue_type)} · conf ${(it.confidence||0).toFixed(2)}</div>
      <div class="issue-detail">
        <div>${esc(it.description || "")}</div>
        <dl>${role}${txt}${measured}${expected}</dl>
        <div class="tags">${tags}</div>
      </div>
    </div>`;
  }).join("");
  list.querySelectorAll(".issue").forEach((node) => {
    node.addEventListener("click", () => setActive(node.dataset.id));
  });
}

function renderOverlays() {
  clearOverlays();
  const wrap = $("#canvasWrap");
  const visible = state.issues.filter(
    (it) => !state.hiddenSev.has(it.severity) && it.element_bbox);
  visible.forEach((it) => {
    const c = SEV[it.severity]?.color || "#999";
    const ov = document.createElement("div");
    ov.className = "overlay";
    ov.dataset.id = it.id;
    ov.style.borderColor = c;
    ov.innerHTML = `<span class="marker" style="background:${c}">${it._num}</span>`;
    ov.addEventListener("click", () => setActive(it.id));
    wrap.appendChild(ov);
  });
  positionOverlays();
}

function positionOverlays() {
  const img = $("#screenshot");
  if (!img.naturalWidth) return;
  const ratio = img.clientWidth / img.naturalWidth;
  document.querySelectorAll(".overlay").forEach((ov) => {
    const it = state.issues.find((x) => x.id === ov.dataset.id);
    if (!it || !it.element_bbox) return;
    const b = it.element_bbox;
    ov.style.left = (b.x * ratio) + "px";
    ov.style.top = (b.y * ratio) + "px";
    ov.style.width = (b.w * ratio) + "px";
    ov.style.height = (b.h * ratio) + "px";
  });
}

function setActive(id) {
  state.activeId = (state.activeId === id) ? null : id;
  renderResults();
  document.querySelectorAll(".overlay").forEach((ov) => {
    ov.classList.toggle("active", ov.dataset.id === state.activeId);
  });
  if (state.activeId) {
    const node = document.querySelector(`.issue[data-id="${state.activeId}"]`);
    if (node) node.scrollIntoView({ behavior: "smooth", block: "nearest" });
    const ov = document.querySelector(`.overlay[data-id="${state.activeId}"]`);
    if (ov) ov.scrollIntoView({ behavior: "smooth", block: "nearest" });
  }
}

function clearOverlays() {
  document.querySelectorAll(".overlay").forEach((o) => o.remove());
}

function showBanner(msg) {
  const b = $("#banner");
  b.textContent = msg; b.className = "banner error";
}
function clearBanner() {
  const b = $("#banner");
  b.textContent = ""; b.className = "hidden";
}
function esc(s) {
  return String(s).replace(/[&<>"']/g, (c) =>
    ({ "&": "&amp;", "<": "&lt;", ">": "&gt;", '"': "&quot;", "'": "&#39;" }[c]));
}

document.addEventListener("DOMContentLoaded", init);
```

- [ ] **Step 2: Commit**

```bash
git add src/ui_defect/web/static/app.js
git commit -m "feat(web): app.js — upload, gọi /analyze, render list + overlay, liên kết 2 chiều, filter"
```

---

### Task 4: HTML — index.html đầy đủ (thay placeholder)

**Files:**
- Modify: `src/ui_defect/web/index.html`
- Test: `tests/unit/test_web_serving.py` (đã có; chỉ chạy lại xác nhận vẫn pass)

- [ ] **Step 1: Ghi đè index.html bằng nội dung đầy đủ**

Replace toàn bộ `src/ui_defect/web/index.html`:
```html
<!doctype html>
<html lang="vi">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>UI Defect Analyzer</title>
  <link rel="stylesheet" href="/static/style.css">
</head>
<body>
  <header>
    <h1>UI Defect Analyzer</h1>
    <button id="toggleAdvanced" class="btn btn-secondary">Cài đặt nâng cao</button>
  </header>

  <div class="uploadbar">
    <div id="dropzone" class="dropzone">Kéo-thả ảnh PNG/JPG vào đây hoặc bấm để chọn file</div>
    <input id="fileInput" type="file" accept="image/png,image/jpeg" class="hidden">

    <div id="advanced" class="controls hidden">
      <label>Platform
        <select id="platform">
          <option value="android" selected>android</option>
          <option value="ios">ios</option>
          <option value="web">web</option>
        </select>
      </label>
      <label>Min severity
        <select id="minSeverity">
          <option value="trivial">trivial</option>
          <option value="low" selected>low</option>
          <option value="medium">medium</option>
          <option value="high">high</option>
          <option value="critical">critical</option>
        </select>
      </label>
      <label>Min confidence
        <input id="minConfidence" type="number" min="0" max="1" step="0.05" value="0.4" style="width:70px">
      </label>
      <label><input id="runVlm" type="checkbox" checked> Chạy VLM</label>
    </div>

    <div class="controls">
      <button id="analyzeBtn" class="btn" disabled>Phân tích</button>
    </div>
  </div>

  <div id="banner" class="hidden"></div>

  <div class="layout">
    <div class="col-image">
      <div id="canvasWrap" class="canvas-wrap hidden">
        <img id="screenshot" alt="screenshot">
      </div>
    </div>
    <div class="col-results">
      <div id="summary" class="summary"></div>
      <div id="issueList" class="issue-list">
        <div class="empty">Chưa có kết quả. Tải ảnh và ấn Phân tích.</div>
      </div>
    </div>
  </div>

  <script src="/static/app.js"></script>
</body>
</html>
```

- [ ] **Step 2: Chạy lại test serving, xác nhận PASS**

Run: `pytest tests/unit/test_web_serving.py -v`
Expected: cả 3 test PASS (vẫn chứa "UI Defect Analyzer").

- [ ] **Step 3: Commit**

```bash
git add src/ui_defect/web/index.html
git commit -m "feat(web): index.html đầy đủ — upload bar, advanced, layout 2 cột"
```

---

### Task 5: Verify thủ công end-to-end + chốt

**Files:** không sửa code (chỉ chạy & quan sát).

- [ ] **Step 1: Chạy full test suite — không vỡ gì**

Run: `pytest -q`
Expected: tất cả test cũ + 3 test mới PASS (≥ 96 pass).

- [ ] **Step 2: Khởi động server**

Run: `uvicorn ui_defect.api.main:app --port 8000` (chạy nền hoặc terminal riêng).
Mở `http://localhost:8000/`.

- [ ] **Step 3: Smoke test giao diện (rule-only, không cần llama.cpp)**

- Tắt "Chạy VLM" (advanced) để không cần llama.cpp server.
- Kéo-thả 1 ảnh screenshot bất kỳ → thấy preview.
- Ấn Phân tích → spinner → danh sách lỗi (nếu rule bắt được) + summary.
- Kiểm: overlay bbox đúng vị trí, số thứ tự + màu severity khớp; click item → highlight box & ngược lại; bấm chip severity → ẩn/hiện đúng nhóm.
- Test ảnh hỏng (đổi tên .txt thành .png) → banner "File không phải ảnh hợp lệ."

- [ ] **Step 4: Cập nhật CLAUDE.md mục 7 (trạng thái)**

Trong `/home/research/CLAUDE.md` mục "7. Trạng thái", thêm dòng đã hoàn thành:
```
- [x] Web UI (`src/ui_defect/web/`) — upload, phân tích, list + overlay Set-of-Marks (F2.0).
```

- [ ] **Step 5: Commit cuối**

```bash
git add CLAUDE.md
git commit -m "docs(F2.0): đánh dấu Web UI hoàn thành ở CLAUDE.md mục 7"
git push
```
```
